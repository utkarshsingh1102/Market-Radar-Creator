version: "3.9"

services:
  forge:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./storage:/app/storage
      - ./assets:/app/assets
    environment:
      - APP_ENV=local
      - STORAGE_BACKEND=local
    restart: unless-stopped
